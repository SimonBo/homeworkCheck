%h1 Homework listing


- @pages.each do |page|
  .panel.panel-primary
    .panel-heading
      %b Title:
      = page.title.html_safe
    .panel-body
      = page.content.html_safe
    = link_to 'Show', page
    = link_to "Mark HW", "/editor" + "/pages/#{page.id}", id: "edit_link", data: { save_url: mercury_update_path(page) }
    = link_to 'Delete', page, :method => :delete, :data => { :confirm => 'Are you sure?' }
    %br

= link_to 'New Page', "/editor/pages/new", id: "new_page", data: { save_url: mercury_new_path }
